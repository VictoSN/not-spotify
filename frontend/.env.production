VITE_USE_MOCK=false
VITE_API_URL=https://api.not-spotify.lol
# Set from serverless\deploy-lambda.ps1 output - see docs/aws-lambda-setup.md
# Requires a backend carrying POST /me/uploads/complete (shipped in task def rev 34).
# Against an older backend, uploads presign and reach S3 and then the completion call
# 404s, so the user sees a failure and the object is orphaned - keep the two in step.
VITE_UPLOADS_API_URL=https://dg607y4cpj.execute-api.ap-southeast-1.amazonaws.com
