// Adds the @testing-library/jest-dom matchers (toBeInTheDocument, etc.) to
// Vitest's expect, for future component tests. Loaded via vite.config test.setupFiles.
import '@testing-library/jest-dom/vitest'
